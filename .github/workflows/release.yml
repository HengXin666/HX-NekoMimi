name: Build & Release APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:
    inputs:
      tag_name:
        description: '版本号 (如: v1.0.0)'
        required: true
        type: string
      release_notes:
        description: '发版描述'
        required: true
        type: string

# 避免同一个 commit 重复触发导致并发冲突
concurrency:
  group: release-${{ github.sha }}
  cancel-in-progress: true

jobs:
  build-and-release:
    name: 打包并发布 APK
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 配置 JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 赋予 Gradle 执行权限
        run: chmod +x gradlew

      - name: 解码签名密钥
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: printf '%s' "$KEYSTORE_BASE64" | base64 -d > ${{ github.workspace }}/nekomimi-release.jks

      - name: 构建 Release APK
        env:
          KEYSTORE_FILE: ${{ github.workspace }}/nekomimi-release.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease

      - name: 设置版本信息
        id: version
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            # 使用时间戳格式: nightly-20260217-1814 (年月日-时分)
            TIMESTAMP=$(TZ='Asia/Shanghai' date +%Y%m%d-%H%M)
            SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
            echo "tag_name=nightly-${TIMESTAMP}" >> $GITHUB_OUTPUT
            echo "release_notes=自动构建版本

📅 构建时间: $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')
🔗 Commit: ${{ github.sha }}

---
非正式发行版本" >> $GITHUB_OUTPUT
          else
            echo "tag_name=${{ github.event.inputs.tag_name }}" >> $GITHUB_OUTPUT
            echo "release_notes=${{ github.event.inputs.release_notes }}" >> $GITHUB_OUTPUT
          fi

      - name: 重命名 APK
        run: |
          mv app/build/outputs/apk/release/app-release.apk \
             app/build/outputs/apk/release/HX-NekoMimi-${{ steps.version.outputs.tag_name }}.apk

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: HX-NekoMimi ${{ steps.version.outputs.tag_name }}
          body: ${{ steps.version.outputs.release_notes }}
          draft: false
          prerelease: ${{ github.event_name == 'push' }}
          files: app/build/outputs/apk/release/HX-NekoMimi-${{ steps.version.outputs.tag_name }}.apk
